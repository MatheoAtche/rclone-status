# systemd user unit for the Rclone Status tray indicator.
# install.sh rewrites ExecStart to this checkout's launcher and installs it
# to ~/.config/systemd/user/.

[Unit]
Description=Rclone Status tray indicator
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/local/bin/rclone-status-tray
Restart=on-failure
RestartSec=5

[Install]
WantedBy=graphical-session.target
